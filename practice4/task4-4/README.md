\# Задание 4-4 (Вариант 5): Анализ памяти и процессов



\## Цель

Изучить утилиты для анализа памяти процессов: `pmap`, `/proc/\[PID]/maps`, `/proc/\[PID]/status`, `valgrind`, `strace`.



\## Выполненные команды



\### 1. pmap — карта памяти процесса

```bash

pmap -x <PID>

